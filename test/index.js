'use strict';

import test from 'tape';

test('Tests run', (assert) => {
  assert.pass('Tests run');
  assert.end();
});

test('<Add your tests>', (assert) => {
  assert.fail('You need to define some tests.');
});
